<!DOCTYPE html>
<html>
  <head>
    <title>Arpcentral // Bpress</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">


    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>


    <nav class="row align-items-center">
      <div class="col col-7">
        <img src="/img/bpress-icon-grey.png">
      </div>
      <div class="col col-5">
        <h1>BPress</h1>
      </div>
    </nav>


    <div class="row">
      <div class="col-sm-12">
      <main>
        <% if flash[:notice] %>
          <div class="row">
            <div class="col-12 text-center">
              <div class="alert alert-warning" role="alert">
              <%= flash[:notice] %>

              <button type="button" class="close" data-dismiss="alert" aria-label="Close">
    <span aria-hidden="true">&times;</span>
  </button>


              </div>
            </div>
          </div>
        <% end %>
        <%= yield %>
      </main>
      </div>
    </div>


    <footer class="row">
      <div class="col-sm-12">
        <p> v1.0 | Arpcentral.net </p>


          <% if logged_in? %>
            <%= link_to fa_icon("sign-out-alt", text: "Sign Out"), session_path('whatevs'), method: :delete %>
          <% else %>
            <%= link_to fa_icon("sign-in-alt", text: "Sign In"), new_session_path %>
          <% end %>


      </div>
    </footer>
  </body>
</html>
